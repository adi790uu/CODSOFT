

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id    String  @id @default(uuid())
  name  String
  email String  @unique
  password String
  salt String
  orders Orders[]
  address String?
  comments Comment[]
  // carts Cart[]
}

model Book {
  id String @id @default(uuid())
  title String @unique
  description String 
  price Int
  rating Int?
  author String
  stock Int @default(0)
  orders Orders[]
  comments Comment[]
  imageUrl String?
  // carts Cart[] @relation("CartToBooks")
}

model Orders {
  id String @id @default(uuid())
  book Book @relation(fields: [bookId], references: [id])
  bookId String
  receiver User @relation(fields: [userId], references: [id])
  userId String
  status Boolean
  quantity Int
}

model Comment {
  id String @id @default(uuid())
  user User @relation(fields: [userId], references: [id])
  userId String
  book Book @relation(fields: [bookId], references: [id])
  bookId String
  description String
}

// model Cart {
//   id String @id @default(uuid())
//   user     User    @relation(fields: [userId], references: [id])
//   userId   String
//   products Book[]  @relation("CartToBooks")
//   bookId String
// }